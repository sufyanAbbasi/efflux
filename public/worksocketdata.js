// source: efflux.proto
/**
 * @fileoverview
 * @enhanceable
 * @suppress {missingRequire} reports error on implicit type usages.
 * @suppress {messageConventions} JS Compiler reports an error if a variable or
 *     field starts with 'MSG_' and isn't a translatable message.
 * @public
 */
// GENERATED CODE -- DO NOT EDIT!
/* eslint-disable */
// @ts-nocheck

goog.provide('proto.efflux.WorkSocketData');

goog.require('jspb.BinaryReader');
goog.require('jspb.BinaryWriter');
goog.require('jspb.Message');
goog.require('proto.efflux.DiffusionSocketData');

/**
 * Generated by JsPbCodeGenerator.
 * @param {Array=} opt_data Optional initial data array, typically from a
 * server response, or constructed directly in Javascript. The array is used
 * in place and becomes part of the constructed object. It is not cloned.
 * If no data is provided, the constructed object will be empty, but still
 * valid.
 * @extends {jspb.Message}
 * @constructor
 */
proto.efflux.WorkSocketData = function(opt_data) {
  jspb.Message.initialize(this, opt_data, 0, -1, null, null);
};
goog.inherits(proto.efflux.WorkSocketData, jspb.Message);
if (goog.DEBUG && !COMPILED) {
  /**
   * @public
   * @override
   */
  proto.efflux.WorkSocketData.displayName = 'proto.efflux.WorkSocketData';
}



if (jspb.Message.GENERATE_TO_OBJECT) {
/**
 * Creates an object representation of this proto.
 * Field names that are reserved in JavaScript and will be renamed to pb_name.
 * Optional fields that are not set will be set to undefined.
 * To access a reserved field use, foo.pb_<name>, eg, foo.pb_default.
 * For the list of reserved names please see:
 *     net/proto2/compiler/js/internal/generator.cc#kKeyword.
 * @param {boolean=} opt_includeInstance Deprecated. whether to include the
 *     JSPB instance for transitional soy proto support:
 *     http://goto/soy-param-migration
 * @return {!Object}
 */
proto.efflux.WorkSocketData.prototype.toObject = function(opt_includeInstance) {
  return proto.efflux.WorkSocketData.toObject(opt_includeInstance, this);
};


/**
 * Static version of the {@see toObject} method.
 * @param {boolean|undefined} includeInstance Deprecated. Whether to include
 *     the JSPB instance for transitional soy proto support:
 *     http://goto/soy-param-migration
 * @param {!proto.efflux.WorkSocketData} msg The msg instance to transform.
 * @return {!Object}
 * @suppress {unusedLocalVariables} f is only used for nested messages
 */
proto.efflux.WorkSocketData.toObject = function(includeInstance, msg) {
  var f, obj = {
    workType: jspb.Message.getFieldWithDefault(msg, 1, 0),
    result: jspb.Message.getFieldWithDefault(msg, 2, ""),
    status: jspb.Message.getFieldWithDefault(msg, 3, 0),
    diffusion: (f = msg.getDiffusion()) && proto.efflux.DiffusionSocketData.toObject(includeInstance, f)
  };

  if (includeInstance) {
    obj.$jspbMessageInstance = msg;
  }
  return obj;
};
}


/**
 * Deserializes binary data (in protobuf wire format).
 * @param {jspb.ByteSource} bytes The bytes to deserialize.
 * @return {!proto.efflux.WorkSocketData}
 */
proto.efflux.WorkSocketData.deserializeBinary = function(bytes) {
  var reader = new jspb.BinaryReader(bytes);
  var msg = new proto.efflux.WorkSocketData;
  return proto.efflux.WorkSocketData.deserializeBinaryFromReader(msg, reader);
};


/**
 * Deserializes binary data (in protobuf wire format) from the
 * given reader into the given message object.
 * @param {!proto.efflux.WorkSocketData} msg The message object to deserialize into.
 * @param {!jspb.BinaryReader} reader The BinaryReader to use.
 * @return {!proto.efflux.WorkSocketData}
 */
proto.efflux.WorkSocketData.deserializeBinaryFromReader = function(msg, reader) {
  while (reader.nextField()) {
    if (reader.isEndGroup()) {
      break;
    }
    var field = reader.getFieldNumber();
    switch (field) {
    case 1:
      var value = /** @type {number} */ (reader.readInt32());
      msg.setWorkType(value);
      break;
    case 2:
      var value = /** @type {string} */ (reader.readString());
      msg.setResult(value);
      break;
    case 3:
      var value = /** @type {number} */ (reader.readInt32());
      msg.setStatus(value);
      break;
    case 4:
      var value = new proto.efflux.DiffusionSocketData;
      reader.readMessage(value,proto.efflux.DiffusionSocketData.deserializeBinaryFromReader);
      msg.setDiffusion(value);
      break;
    default:
      reader.skipField();
      break;
    }
  }
  return msg;
};


/**
 * Serializes the message to binary data (in protobuf wire format).
 * @return {!Uint8Array}
 */
proto.efflux.WorkSocketData.prototype.serializeBinary = function() {
  var writer = new jspb.BinaryWriter();
  proto.efflux.WorkSocketData.serializeBinaryToWriter(this, writer);
  return writer.getResultBuffer();
};


/**
 * Serializes the given message to binary data (in protobuf wire
 * format), writing to the given BinaryWriter.
 * @param {!proto.efflux.WorkSocketData} message
 * @param {!jspb.BinaryWriter} writer
 * @suppress {unusedLocalVariables} f is only used for nested messages
 */
proto.efflux.WorkSocketData.serializeBinaryToWriter = function(message, writer) {
  var f = undefined;
  f = message.getWorkType();
  if (f !== 0) {
    writer.writeInt32(
      1,
      f
    );
  }
  f = message.getResult();
  if (f.length > 0) {
    writer.writeString(
      2,
      f
    );
  }
  f = message.getStatus();
  if (f !== 0) {
    writer.writeInt32(
      3,
      f
    );
  }
  f = message.getDiffusion();
  if (f != null) {
    writer.writeMessage(
      4,
      f,
      proto.efflux.DiffusionSocketData.serializeBinaryToWriter
    );
  }
};


/**
 * optional int32 work_type = 1;
 * @return {number}
 */
proto.efflux.WorkSocketData.prototype.getWorkType = function() {
  return /** @type {number} */ (jspb.Message.getFieldWithDefault(this, 1, 0));
};


/**
 * @param {number} value
 * @return {!proto.efflux.WorkSocketData} returns this
 */
proto.efflux.WorkSocketData.prototype.setWorkType = function(value) {
  return jspb.Message.setProto3IntField(this, 1, value);
};


/**
 * optional string result = 2;
 * @return {string}
 */
proto.efflux.WorkSocketData.prototype.getResult = function() {
  return /** @type {string} */ (jspb.Message.getFieldWithDefault(this, 2, ""));
};


/**
 * @param {string} value
 * @return {!proto.efflux.WorkSocketData} returns this
 */
proto.efflux.WorkSocketData.prototype.setResult = function(value) {
  return jspb.Message.setProto3StringField(this, 2, value);
};


/**
 * optional int32 status = 3;
 * @return {number}
 */
proto.efflux.WorkSocketData.prototype.getStatus = function() {
  return /** @type {number} */ (jspb.Message.getFieldWithDefault(this, 3, 0));
};


/**
 * @param {number} value
 * @return {!proto.efflux.WorkSocketData} returns this
 */
proto.efflux.WorkSocketData.prototype.setStatus = function(value) {
  return jspb.Message.setProto3IntField(this, 3, value);
};


/**
 * optional DiffusionSocketData diffusion = 4;
 * @return {?proto.efflux.DiffusionSocketData}
 */
proto.efflux.WorkSocketData.prototype.getDiffusion = function() {
  return /** @type{?proto.efflux.DiffusionSocketData} */ (
    jspb.Message.getWrapperField(this, proto.efflux.DiffusionSocketData, 4));
};


/**
 * @param {?proto.efflux.DiffusionSocketData|undefined} value
 * @return {!proto.efflux.WorkSocketData} returns this
*/
proto.efflux.WorkSocketData.prototype.setDiffusion = function(value) {
  return jspb.Message.setWrapperField(this, 4, value);
};


/**
 * Clears the message field making it undefined.
 * @return {!proto.efflux.WorkSocketData} returns this
 */
proto.efflux.WorkSocketData.prototype.clearDiffusion = function() {
  return this.setDiffusion(undefined);
};


/**
 * Returns whether this field is set.
 * @return {boolean}
 */
proto.efflux.WorkSocketData.prototype.hasDiffusion = function() {
  return jspb.Message.getField(this, 4) != null;
};


